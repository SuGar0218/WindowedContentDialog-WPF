<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SuGarToolkit.WPF.Controls.Dialogs">

    <Style x:Key="ContentDialogWindowDefaultStyle" TargetType="{x:Type local:ContentDialogWindow}">
        <Setter Property="SizeToContent" Value="WidthAndHeight" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ContentDialogWindow}">
                    <local:ContentDialogContent
                        x:Name="ContentDialogContent"
                        Title="{TemplateBinding DialogTitle}"
                        CloseButtonStyle="{TemplateBinding CloseButtonStyle}"
                        CloseButtonText="{TemplateBinding CloseButtonText}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                        DefaultButton="{TemplateBinding DefaultButton}"
                        Foreground="{TemplateBinding Foreground}"
                        IsPrimaryButtonEnabled="{TemplateBinding IsPrimaryButtonEnabled}"
                        IsSecondaryButtonEnabled="{TemplateBinding IsSecondaryButtonEnabled}"
                        PrimaryButtonStyle="{TemplateBinding PrimaryButtonStyle}"
                        PrimaryButtonText="{TemplateBinding PrimaryButtonText}"
                        SecondaryButtonStyle="{TemplateBinding SecondaryButtonStyle}"
                        SecondaryButtonText="{TemplateBinding SecondaryButtonText}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<Style BasedOn="{StaticResource ContentDialogWindowDefaultStyle}" TargetType="{x:Type local:ContentDialogWindow}" />-->

</ResourceDictionary>